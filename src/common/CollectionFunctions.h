#ifndef COLLECTIONFUNCTIONS_H
#define COLLECTIONFUNCTIONS_H

#include "../utils/templateUtils.h"

#define at(Type) concat(at,Type)

#define length(Type) concat(length,Type)

#define size(Type) concat(size,Type)

#define capacity(Type) concat(capacity,Type)

#define back(Type) concat(back,Type)

#define front(Type) concat(front,Type)

#define push_back(Type) concat(push_back,Type)

#define pop_back(Type) concat(pop_back,Type)

#define push_front(Type) concat(push_front,Type)

#define pop_front(Type) concat(pop_front,Type)

#endif // COLLECTIONFUNCTIONS_H
